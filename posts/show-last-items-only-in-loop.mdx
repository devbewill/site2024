---
title: Show last items only in a loop
date: "2023.12.21"
description: Easy way to show last items only in javascript.
tags: ["code", "code3"]
---

If you want to show only the most recent posts (for example, the _last posts_ section for the homepage), you can easily use the javascript `slice` method.

change from:

```javascript
      {AllPosts.map((post) =>
```

to:

```javascript
      {AllPosts.slice(0, 5).map((post) =>
```
